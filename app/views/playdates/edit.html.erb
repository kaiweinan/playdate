<%= form_for @playdate do |p| %>
    <div>
    <%= p.label :title %>
    <%= p.text_field :title %>
    </div>

    <div>
    <%= p.label :playdate %>
    <%= p.date_field :playdate %>
    </div>

    <%= p.submit "Change Playdate!" %>

    <% end %>